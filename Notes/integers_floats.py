"""price = float( input("How much does it cost: $"))

sales_tax = float( input("How much is the sales tax: %"))/100

total = rpund(price + (price * sales_tax), 2) # or I can do it like: total = round( price + tax, 2)

print(total)"""

apples = int(input("how many apples are there: "))

friends = 8

print("Each frind can have", apples//friends, ", And I have", apples%friends, "Left over")